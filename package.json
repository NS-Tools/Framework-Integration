{
  "name": "ns-tools-integration",
  "version": "1.0.0",
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@hitc/netsuite-types": "^2025.2.10",
    "@oracle/suitecloud-unit-testing": "^1.7.0",
    "@types/jest": "29.5.14",
    "jest": "29.7.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "lint": "npx @biomejs/biome check --staged",
    "lint:fix": "npx @biomejs/biome check --staged --write",
    "lint:all": "npx @biomejs/biome check",
    "lint:all:fix": "npx @biomejs/biome check --write",
    "test": "jest",
    "build": "./build.sh",
    "build:deploy": "npm run build && npx suitecloud project:deploy --validate",
    "update": "git pull origin && npm run update:framework && npm install",
    "update:framework": "cd src/Framework && git pull && git submodule update --merge --recursive && cd ../../"
  }
}
